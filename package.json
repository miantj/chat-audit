{
  "name": "chat-audit",
  "type": "module",
  "private": true,
  "description": "一手聊天审计导出工具",
  "scripts": {
    "start": "pnpm --dir electron start",
    "export": "node chat-audit-export/scripts/start-export.mjs",
    "prepare-runtime": "pnpm --dir electron prepare-runtime",
    "build": "pnpm --dir electron build",
    "build:mac": "pnpm --dir electron build:mac",
    "build:dir": "pnpm --dir electron build:dir",
    "release": "pnpm --dir electron release",
    "release:mac": "pnpm --dir electron release:mac",
    "release:upload:mac": "pnpm --dir electron release:upload:mac",
    "release:win": "pnpm --dir electron release:win",
    "release:upload:win": "pnpm --dir electron release:upload:win"
  }
}
